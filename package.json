{
  "name": "googlechart",
  "version": "1.0.0",
  "description": "",
  "main": "test.js",
  "scripts": {
    "build": "browserify index.js -o build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "linfeng.dong",
  "license": "ISC",
  "dependencies": {
    "express": "latest"
  }
}
